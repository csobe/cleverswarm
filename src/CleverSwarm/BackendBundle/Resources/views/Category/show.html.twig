{% extends "CleverSwarmBackendBundle::layout.html.twig" %}


{% block h1 %}{{category.name}}{% endblock %}

{% block content %}

    <!-- Content Row -->
    <div class="row">

        <!-- Blog Post Content Column -->
        <div class="col-lg-12">

            <p>Nom de la catégorie: {{category.name}}</p>
        </div>

    </div>
    <!-- /.row -->   

    <ul>
        <li>
            <a href="{{ path('cleverswarm_backend_category_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cleverswarm_backend_category_edit', { 'id': category.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
 
{% endblock %}
