{% extends "CleverSwarmBackendBundle::layout.html.twig" %}

{% block h1 %}{{slide.caption}}{% endblock %}

{% block content %}


        <!-- Content Row -->
        <div class="row">
            <div class="col-md-12">
                <img src="{{slide.img}}" alt="" class="img-responsive">
            </div>
        </div>
        <!-- /.row -->   

        <ul>
            <li>
                <a href="{{ path('cleverswarm_backend_slide_index') }}">Back to the list</a>
            </li>
            <li>
                <a href="{{ path('cleverswarm_backend_slide_edit', { 'id': slide.id }) }}">Edit</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                    <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
 
{% endblock %}
