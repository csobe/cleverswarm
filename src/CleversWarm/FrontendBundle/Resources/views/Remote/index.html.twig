{% extends 'CleverSwarmFrontendBundle::layout.html.twig' %}


{% block h1 %}Contrôle à distance{% endblock %}
{% block content %}
   {% for remote in remotes %}
        <!-- Project -->
        <div class="row">
            <div class="col-md-7">
                <a href="{{path('cleverswarm_frontend_remote_show',{ 'id': remote.id })}}">
                    <img class="img-responsive img-hover" src="http://placehold.it/700x300" alt="">
                </a>
            </div>
            <div class="col-md-5">
                <h3>{{remote.name}}</h3>
                <h4>Proposé par {{remote.user}}</h4>
                <p>{{remote.description}}</p>
                <a class="btn btn-primary" href="{{path('cleverswarm_frontend_remote_show',{ 'id': remote.id })}}">Plus de détails</i></a>
            </div>
        </div>
        <!-- /.row -->

        <hr>
    {% endfor %}

    <!-- Pagination -->
    <div class="row text-center">
        <div class="col-lg-12">
            <ul class="pagination">
                <li>
                    <a href="#">&laquo;</a>
                </li>
                <li class="active">
                    <a href="#">1</a>
                </li>
                <li>
                    <a href="#">2</a>
                </li>
                <li>
                    <a href="#">3</a>
                </li>
                <li>
                    <a href="#">4</a>
                </li>
                <li>
                    <a href="#">5</a>
                </li>
                <li>
                    <a href="#">&raquo;</a>
                </li>
            </ul>
        </div>
    </div>
    <!-- /.row -->

{% endblock %}
